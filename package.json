{
  "name": "claude-session-restore",
  "version": "0.1.0",
  "description": "Restore your Claude Code CLI sessions in VSCode after a shutdown, and manage/delete chats across every project.",
  "type": "module",
  "bin": {
    "csr": "./bin/csr.js"
  },
  "scripts": {
    "test": "TZ=Asia/Kolkata node --test test/*.test.js",
    "gui": "electron src/gui/main.js",
    "daemon": "node src/recorder/daemon.js"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "MIT",
  "private": true,
  "dependencies": {
    "electrobun": "^1.18.1"
  }
}
